query GetCharacterDetail($id: ID!) {
  character(id: $id) {
    id
    name
    status
    species
    type
    gender
    image
    origin {
      id
      name
      dimension
    }
    location {
      id
      name
      dimension
    }
    episode {
      id
      name
      air_date
      episode
    }
  }
}